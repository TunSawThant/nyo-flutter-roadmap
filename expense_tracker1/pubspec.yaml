name: expense_tracker
description: A command-line expense tracker written in Dart.
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  intl: ^0.19.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
